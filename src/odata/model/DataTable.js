var akeraApi = require('akera-api');
var util = require('../util.js');

function DataTable() {
}

DataTable.prototype.read = function(broker, query, cb) {
   
};

DataTable.prototype.create = function(broker, table, data) {
   
};

DataTable.prototype.update = function(broker, table, filter, data, cb) {
   
};

DataTable.prototype.destroy = function(broker, table, filter, data, cb) {
   
};

DataTable.prototype.save = function(broker, table, data, cb) {
   
};

module.exports = DataTable;